# Optional dev overrides — Jenkins pipeline uses values.yaml directly, not this file
# Dev environment overrides
backend:
  image:
    tag: latest
  replicaCount: 1

frontend:
  image:
    tag: latest
  replicaCount: 1

database:
  storage: 1Gi

namespace: sports-betting-dev