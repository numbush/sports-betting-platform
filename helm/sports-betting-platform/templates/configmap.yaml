# Non-secret backend config — credentials live in database-secret
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: {{ .Values.namespace }}
data:
  DB_HOST: "database"
  DB_PORT: "5432"
  DB_NAME: {{ .Values.database.credentials.name }}