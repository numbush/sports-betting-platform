apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: database
  ports:
  - port: {{ .Values.database.service.port }}
    targetPort: 5432
  type: ClusterIP