# Default-path ingress: /api/* → backend, / → frontend (requires nginx ingress controller in cluster)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sports-betting-ingress
  namespace: sports-betting-dev
  annotations:
    # Rewrites /api/games → /games on the backend service (strips the /api prefix)
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /api/(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: backend
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80