apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: sports-betting-dev
data:
  # "database" resolves via K8s DNS to the database Service in the same namespace
  DB_HOST: "database"
  DB_PORT: "5432"
  DB_NAME: "sportsdb"